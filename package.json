{
  "name": "meteo-station-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "kill:5173": "npx kill-port 5173",
    "ports": "node bridge/list-ports.js",
    "dash:replay": "node bridge/replay-server.js",
    "dash:live": "MODE=live BAUD=115200 node bridge/replay-server.js",
    "dash:dev": "nodemon bridge/replay-server.js",
    "pw:smoke": "start-server-and-test \"cross-env REPLAY_PATH=replay/fixtures/one-frame-happy-smoke.ndjson npm run dash:replay\" http://localhost:5173 \"cross-env NO_WEBSERVER=1 npx playwright test tests/smoke.spec.ts\"",
    "pw:replay": "start-server-and-test \"cross-env REPLAY_PATH=replay/fixtures/replay.ndjson REPLAY_INTERVAL_MS=1200 npm run dash:replay\" http://localhost:5173 \"cross-env NO_WEBSERVER=1 npx playwright test tests/replay.spec.ts\"",
    "pw:formatting": "start-server-and-test \"cross-env REPLAY_PATH=replay/fixtures/empty.ndjson npm run dash:replay\" http://localhost:5173 \"cross-env NO_WEBSERVER=1 npx playwright test --project=chromium tests/formatting.spec.ts\"",
    "pw:derived:hi": "start-server-and-test \"cross-env REPLAY_PATH=replay/fixtures/derived-heatindex.ndjson npm run dash:replay\" http://localhost:5173 \"cross-env NO_WEBSERVER=1 npx playwright test --project=chromium tests/derived.spec.ts  --grep @hi\"",
    "pw:derived:dew": "start-server-and-test \"cross-env REPLAY_PATH=replay/fixtures/derived-dewpoint.ndjson npm run dash:replay\" http://localhost:5173 \"cross-env NO_WEBSERVER=1 npx playwright test --project=chromium tests/derived.spec.ts --grep @dew \""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/petromirkolev/meteo-station-dashboard.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/petromirkolev/meteo-station-dashboard/issues"
  },
  "homepage": "https://github.com/petromirkolev/meteo-station-dashboard#readme",
  "dependencies": {
    "@serialport/parser-readline": "^13.0.0",
    "serialport": "^13.0.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.1.0",
    "cross-env": "^10.1.0",
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.11",
    "start-server-and-test": "^2.1.3",
    "typescript": "^5.9.3"
  }
}
